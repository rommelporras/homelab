# manifests/karakeep/secret.yaml
# Documentation placeholder - secrets are created imperatively from 1Password
#
# Create with:
#   eval $(op signin)
#
#   kubectl-homelab create secret generic karakeep-secrets \
#     --namespace karakeep \
#     --from-literal=nextauth-secret="$(op read 'op://Kubernetes/Karakeep/nextauth-secret')" \
#     --from-literal=meili-master-key="$(op read 'op://Kubernetes/Karakeep/meili-master-key')"
#
---
apiVersion: v1
kind: Secret
metadata:
  name: karakeep-secrets
  namespace: karakeep
  labels:
    app: karakeep
    app.kubernetes.io/part-of: karakeep
  annotations:
    secret-source: "op://Kubernetes/Karakeep"
    managed-by: "imperative-kubectl"
type: Opaque
# DATA INTENTIONALLY OMITTED - Created imperatively from 1Password
# stringData:
#   nextauth-secret: <never commit this>
#   meili-master-key: <never commit this>
