# ARR Backup CronJob: sonarr
# Per-app backup with podAffinity (follows app pod to any node)
# Schedule: 02:25 Manila | Retention: 3 days | NFS: 10.10.30.4
---
apiVersion: batch/v1
kind: CronJob
metadata:
  name: arr-backup-sonarr
  namespace: arr-stack
  labels:
    app: arr-backup-sonarr
    app.kubernetes.io/part-of: arr-stack
spec:
  schedule: "25 2 * * *"
  timeZone: "Asia/Manila"
  concurrencyPolicy: Forbid
  successfulJobsHistoryLimit: 3
  failedJobsHistoryLimit: 3
  jobTemplate:
    spec:
      backoffLimit: 0
      activeDeadlineSeconds: 600
      template:
        metadata:
          labels:
            app: arr-backup-sonarr
        spec:
          restartPolicy: Never
          automountServiceAccountToken: false
          affinity:
            podAffinity:
              requiredDuringSchedulingIgnoredDuringExecution:
                - labelSelector:
                    matchLabels:
                      app: sonarr
                  topologyKey: kubernetes.io/hostname
          securityContext:
            runAsUser: 0
            runAsGroup: 0
            seccompProfile:
              type: RuntimeDefault
          containers:
            - name: backup
              image: keinos/sqlite3:3.46.1
              command: ["/bin/sh", "-c"]
              args:
                - |
                  set -e
                  export TZ=UTC-8
                  TIMESTAMP=$(date +%Y%m%d-%H%M%S)
                  echo "=== Sonarr ==="
                  mkdir -p /backup/sonarr
                  find /config \( -name "*.db" -o -name "*.sqlite3" \) -not -name "*-shm" -not -name "*-wal" -not -path "*/lost+found/*" -type f | while read db; do
                    DBNAME=$(basename "$db")
                    sqlite3 "file:${db}?immutable=1" ".backup /backup/sonarr/${DBNAME%.*}-${TIMESTAMP}.db"
                    echo "  SQLite backup: $DBNAME"
                  done
                  find /backup/sonarr -name "*-*.db" -mtime +3 -delete 2>/dev/null || true
                  echo "Backup complete (sonarr)"
              securityContext:
                allowPrivilegeEscalation: false
                readOnlyRootFilesystem: true
                capabilities:
                  drop: ["ALL"]
              resources:
                requests: { cpu: 50m, memory: 64Mi }
                limits: { cpu: 200m, memory: 256Mi }
              volumeMounts:
                - name: config
                  mountPath: /config
                - name: backup
                  mountPath: /backup
          volumes:
            - name: config
              persistentVolumeClaim:
                claimName: sonarr-config
            - name: backup
              nfs:
                server: 10.10.30.4
                path: /Kubernetes/Backups/arr-configs
