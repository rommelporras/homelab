# Recommendarr Service
# ClusterIP — accessed via Gateway API HTTPRoute
---
apiVersion: v1
kind: Service
metadata:
  name: recommendarr
  namespace: arr-stack
  labels:
    app: recommendarr
spec:
  type: ClusterIP
  selector:
    app: recommendarr
  ports:
    - name: http
      port: 3000
      targetPort: http
