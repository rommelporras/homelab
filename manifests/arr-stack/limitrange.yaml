apiVersion: v1
kind: LimitRange
metadata:
  name: default-limits
  namespace: arr-stack
spec:
  limits:
    - default:
        cpu: 1000m
        memory: 1Gi
      defaultRequest:
        cpu: 100m
        memory: 256Mi
      type: Container
