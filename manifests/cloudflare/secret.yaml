# manifests/cloudflare/secret.yaml
# Documentation placeholder - secret is created imperatively from 1Password
#
# Create with:
#   kubectl-homelab create secret generic cloudflared-token \
#     --from-literal=token="$(op read 'op://Kubernetes/Cloudflare Tunnel/token')" \
#     -n cloudflare
#
---
apiVersion: v1
kind: Secret
metadata:
  name: cloudflared-token
  namespace: cloudflare
  labels:
    app: cloudflared
  annotations:
    # Document where the real secret lives
    secret-source: "op://Kubernetes/Cloudflare Tunnel/token"
    managed-by: "imperative-kubectl"
type: Opaque
# DATA INTENTIONALLY OMITTED - Created imperatively from 1Password
# stringData:
#   token: <never commit this>
