apiVersion: v1
kind: ResourceQuota
metadata:
  name: resource-quota
  namespace: atuin
spec:
  hard:
    requests.cpu: "500m"
    requests.memory: 1Gi
    limits.cpu: "2"
    limits.memory: 3Gi
    pods: "10"
    persistentvolumeclaims: "5"
