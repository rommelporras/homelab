apiVersion: v1
kind: ResourceQuota
metadata:
  name: resource-quota
  namespace: ai
spec:
  hard:
    requests.cpu: "2"
    requests.memory: 4Gi
    limits.cpu: "8"
    limits.memory: 10Gi
    pods: "5"
    persistentvolumeclaims: "5"
