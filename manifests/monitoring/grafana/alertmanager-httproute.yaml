# HTTPRoute for Alertmanager UI - exposes via Gateway API
# URL: https://alertmanager.k8s.rommelporras.com
#
# Internal only — homelab-gateway (10.10.30.20) is not exposed to the internet.
# Alertmanager has no built-in auth; safe behind internal gateway.
---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: alertmanager
  namespace: monitoring
spec:
  parentRefs:
    - name: homelab-gateway
      namespace: default
      sectionName: https

  hostnames:
    - alertmanager.k8s.rommelporras.com

  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /
      backendRefs:
        - name: prometheus-kube-prometheus-alertmanager
          port: 9093
