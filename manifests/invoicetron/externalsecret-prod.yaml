# ExternalSecrets for Invoicetron Prod
# Phase 4.29: Replaces imperative kubectl create secret for invoicetron-prod namespace
#
# 1. invoicetron-db — Vault: secret/invoicetron-prod/db (1 key)
# 2. invoicetron-app — Vault: secret/invoicetron-prod/app (2 keys)
---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: invoicetron-db
  namespace: invoicetron-prod
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: vault-backend
    kind: ClusterSecretStore
  target:
    name: invoicetron-db
  data:
    - secretKey: postgres-password
      remoteRef:
        key: invoicetron-prod/db
        property: postgres-password
---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: invoicetron-app
  namespace: invoicetron-prod
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: vault-backend
    kind: ClusterSecretStore
  target:
    name: invoicetron-app
  data:
    - secretKey: database-url
      remoteRef:
        key: invoicetron-prod/app
        property: database-url
    - secretKey: better-auth-secret
      remoteRef:
        key: invoicetron-prod/app
        property: better-auth-secret
